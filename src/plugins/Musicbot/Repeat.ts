/** @format */

export class Repeat {}
/**
 * TODO: Make a queue repeat command
 * ? This needs the Play command to be rewritten

 * ? Right now the Play command is hard set on deleting the doc after its been read
 * ? Repeat would need the doc to stay there, and to be toggled to that one doc or the whole collection

 * ! This is probably not gonna just be a drop in module for the bot.
 */
